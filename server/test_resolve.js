const { default: makeWASocket, useMultiFileAuthState } = require('@whiskeysockets/baileys'); async function run() { const { state } = await useMultiFileAuthState('./baileys_auth_info'); const sock = makeWASocket({ auth: state }); if (sock.resolveJid) console.log('resolveJid exists!'); if (sock.getBusinessProfile) console.log('getBusinessProfile exists!'); if (sock.getUser) console.log('getUser exists!'); process.exit(0); } run();
